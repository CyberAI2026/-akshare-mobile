# 2026-09-05 系统研究与维护日志

## 当日结论

- 淘股吧市场观点任务已从单一长进程拆为 5 个并行正文批次、1 个汇总任务和独立微信发送任务；各计算任务硬上限 10 分钟。
- 当日补跑成功：处理 9 篇可访问公开文章，生成面向 2026-09-07 的观点摘要，OpenAI 汇总成功，PushPlus 业务回执码 200。
- 非交易日仍执行公开热帖采集与观点汇总；观点内容明确作为主观研究材料，不替代行情事实，也不直接构成正式推荐。
- 股票—板块映射完成首次可用建库并完成归因；推荐反馈任务已恢复。

## 数据与产物验收

| 项目 | 结果 |
|---|---|
| 板块映射状态 | `PARTIAL_READY`，允许使用已核验部分 |
| 主股票池 | 573 只 |
| 已映射股票 | 567 只（98.95%） |
| 行业覆盖率 | 98.95% |
| 概念覆盖率 | 59.86% |
| 映射关系 | 1,852 条 |
| 多概念股票 | 232 只 |
| 板块事实数 | 477 |
| 推荐跟踪 | 累计 1 只，跟踪中 1 只 |
| D+3 / D+5 / D+10 | 尚未到期，样本均为 0 |

概念覆盖尚未达到正式完整标准，因此必须继续展示为“部分可用”；不得把缺失概念补猜为事实，也不得倒推历史归因。

## 故障与修复

1. 2026-09-04 原市场观点定时任务实际延迟进入队列，并在正文挖掘阶段运行约 75 分钟后超时，未执行 OpenAI 汇总和微信发送。
2. 新流程将最多 20 篇文章按 5 个批次并行，每批最多 4 篇；批次、汇总、发送均为独立短任务。
3. 增加 21:00、21:20、21:40 三次短发送窗口，并以摘要指纹回执保证同一摘要只发送一次。
4. 批次间仅传递标题、链接、正文哈希和结构化分析，不保存文章原文。
5. 板块归因曾因当日观点字段为 `null` 触发类型错误。已改为无观点时使用空证据继续归因，并增加回归测试。
6. GitHub Actions 自身提交不会再次触发下游 push 工作流。板块建库工作流现已直接串接独立归因任务，重新检出最新 `main` 后运行。
7. 本地 19 项联合测试和修复后的 8 项回归测试通过；Python 语法、工作流 YAML 与差异检查通过。

## 用户规则与偏好

- 600801 当前名称统一使用“华新建材”，旧名“华新水泥”仅可作为历史别名；身份匹配始终以股票代码为主。
- 已进入真实持仓且未卖出、未止损或未止盈的股票，不得再次进入新推荐名单。
- 交易页应优先列出当日推荐股票并自动带出代码、现名和参考信息；用户填写实际买入数量和价格。数量为 0 或次日开盘前未填表示未交易，但允许事后补录真实成交日期、价格和数量。
- 买入、卖出及补录都应进入统一交易流水，由系统计算当前持仓；聊天记录不能作为唯一账本。
- 用户接受对交易数据做适度保护，不要求过度隐私化；仓库当前仍为公开，因此真实持仓明细在访问密钥未配置前不写入公开数据路径。
- 用户希望长工作拆成若干小任务，完成一项再进入下一项，单个计算任务控制在 10 分钟以内。

## 已知边界与待办

- Streamlit“交易与持仓”页代码已存在，但在 `TRADING_DATA_KEY` 未同时配置到 Streamlit Secrets 和 GitHub Actions Secrets 时保持禁用；密钥配置需要用户账户侧操作。
- 用户报告的 HTTP 410 未在仓库代码和 Actions 日志中复现；当前证据指向私有 Streamlit 的一次性云浏览器授权链接过期。若应用表单内部再次出现 410，需记录具体页面和操作位置再定位。
- 概念板块覆盖率仍为 59.86%，继续以备用源和增量刷新改善；达到门槛前保持部分可用标签。
- 需要继续审查盘后联合分析与其他尚存长流程，将真正耗时的网络/模型步骤进一步拆分；尾盘等待采样属于时点等待，不与计算超时混为一谈。
- 华新建材实际交易（用户口述：2026-09-04 买入 2,100 股，成交价 24.80 元）尚未写入交易账本，待交易数据密钥启用后录入并核对。

